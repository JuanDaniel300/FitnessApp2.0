import { SafeAreaView, Text } from "react-native";
import { View } from "react-native";

const Result = () => {
  return (
    <SafeAreaView>
      <Text>Results</Text>
    </SafeAreaView>
  );
};

export default Result;
